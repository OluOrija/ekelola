---
const nav = [
  { href: "/", label: "Home" },
  { href: "/blog", label: "Blog" },
  { href: "/tags", label: "Tags" },
  { href: "/archive", label: "Archive" },
  { href: "/about", label: "About" },
];
const { title = "ekelola", description = "AI • Tech • Mind", canonical } = Astro.props;
const site = Astro.site?.toString() ?? "";
---
<header class="border-b">
  <div class="mx-auto max-w-3xl flex items-center justify-between px-4 py-4">
    <a href="/" class="font-bold">{title}</a>
    <nav class="flex gap-6">
      {nav.map((n) => <a href={n.href} class="hover:underline">{n.label}</a>)}
    </nav>
  </div>
</header>

